info:
  name: Agendar Consulta
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/consultas"
  body:
    type: json
    data: |-
      {
        "idCliente": 4,
        "idDoctor": 2,
        "idEspecialidad": 2,
        "idSeguroCliente": 4,
        "fecha": "2026-03-21",
        "prioridad": 1
      }
  auth:
    type: bearer
    token: "{{clienteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
