info:
  name: Agendar Consulta
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/consultas"
  auth:
    type: bearer
    token: "{{clienteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
