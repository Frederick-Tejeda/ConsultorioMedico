info:
  name: Obtener Historial Paciente
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/consultas/paciente/1"
  auth:
    type: bearer
    token: "{{clienteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
