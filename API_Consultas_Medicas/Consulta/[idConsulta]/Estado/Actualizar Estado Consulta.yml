info:
  name: Actualizar Estado Consulta
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseUrl}}/consultas/1/estado"
  auth:
    type: bearer
    token: "{{clienteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
