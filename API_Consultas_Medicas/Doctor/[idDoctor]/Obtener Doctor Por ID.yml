info:
  name: Obtener Doctor Por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/doctores/2"
  auth:
    type: bearer
    token: "{{administradorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
