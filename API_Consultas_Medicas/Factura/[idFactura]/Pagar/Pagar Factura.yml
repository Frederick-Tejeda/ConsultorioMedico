info:
  name: Pagar Factura
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseUrl}}/facturas/3/pagar"
  auth:
    type: bearer
    token: "{{cajaToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
