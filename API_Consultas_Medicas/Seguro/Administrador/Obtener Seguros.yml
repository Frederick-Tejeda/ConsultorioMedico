info:
  name: Obtener Seguros
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/seguros/administrador"
  auth:
    type: bearer
    token: "{{administradorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
