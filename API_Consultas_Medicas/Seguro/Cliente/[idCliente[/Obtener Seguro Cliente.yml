info:
  name: Obtener Seguro Cliente
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/seguros/cliente/4"
  auth:
    type: bearer
    token: "{{clienteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
