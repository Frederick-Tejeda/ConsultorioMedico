info:
  name: Obtener Clientes
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/usuarios/administrador"
  auth:
    type: bearer
    token: "{{administradorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
