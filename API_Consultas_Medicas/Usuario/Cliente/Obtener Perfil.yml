info:
  name: Obtener Perfil
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/usuarios/cliente/8/perfil"
  auth:
    type: bearer
    token: "{{clienteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
