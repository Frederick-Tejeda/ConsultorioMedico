info:
  name: Actualizar Usuario
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/usuarios/cliente/8"
  body:
    type: json
    data: |-
      { 
        "correo": "ejemplo@ejemplo.ejemplo", 
        "password": "MiPassword123" 
      }
  auth:
    type: bearer
    token: "{{clienteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
