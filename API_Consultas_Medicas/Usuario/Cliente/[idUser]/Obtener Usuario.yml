info:
  name: Obtener Usuario
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/usuarios/cliente/9"
  auth:
    type: bearer
    token: "{{administradorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
