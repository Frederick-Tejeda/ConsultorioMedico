info:
  name: Obtener Usuario
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/usuarios/cliente/8"
  auth:
    type: bearer
    token: "{{administradorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
